function fn(){
    var config = {
    baseUrl: 'https://restful-booker.herokuapp.com',

    endpointAuth: '/auth',
    endpointBooking: '/booking',
    endpointPing: '/ping',
    username: 'admin',
    password: 'password123'
    };

    return config;
}